/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TodoInput } from '../../models/todo-input';
import { TodoOutput } from '../../models/todo-output';

export interface Atualizar$Params {

/**
 * ID da tarefa
 */
  id: number;
      body: TodoInput
}

export function atualizar(http: HttpClient, rootUrl: string, params: Atualizar$Params, context?: HttpContext): Observable<StrictHttpResponse<TodoOutput>> {
  const rb = new RequestBuilder(rootUrl, atualizar.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TodoOutput>;
    })
  );
}

atualizar.PATH = '/api/todos/{id}';
