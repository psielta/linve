/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CategoriaOutput } from '../../models/categoria-output';

export interface Listar2$Params {

/**
 * ID da culinaria para filtro opcional
 */
  id_culinaria?: number;
}

export function listar2(http: HttpClient, rootUrl: string, params?: Listar2$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CategoriaOutput>>> {
  const rb = new RequestBuilder(rootUrl, listar2.PATH, 'get');
  if (params) {
    rb.query('id_culinaria', params.id_culinaria, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CategoriaOutput>>;
    })
  );
}

listar2.PATH = '/api/categorias';
