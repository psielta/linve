/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MunicipioOutput } from '../../models/municipio-output';

export interface ListarMunicipios$Params {
  ufSigla: string;
}

export function listarMunicipios(http: HttpClient, rootUrl: string, params: ListarMunicipios$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MunicipioOutput>>> {
  const rb = new RequestBuilder(rootUrl, listarMunicipios.PATH, 'get');
  if (params) {
    rb.query('siglaUf', params.ufSigla, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MunicipioOutput>>;
    })
  );
}

listarMunicipios.PATH = '/api/dados-abertos/ufs/municipios';
