/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdicionalInput } from '../../models/adicional-input';
import { AdicionalOutput } from '../../models/adicional-output';

export interface Atualizar5$Params {
  id: number;
      body: AdicionalInput
}

export function atualizar5(http: HttpClient, rootUrl: string, params: Atualizar5$Params, context?: HttpContext): Observable<StrictHttpResponse<AdicionalOutput>> {
  const rb = new RequestBuilder(rootUrl, atualizar5.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/problem+json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdicionalOutput>;
    })
  );
}

atualizar5.PATH = '/api/adicionais/{id}';
