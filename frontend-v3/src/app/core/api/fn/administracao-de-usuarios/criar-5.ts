/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserAdminInput } from '../../models/user-admin-input';
import { UserPasswordResetOutput } from '../../models/user-password-reset-output';

export interface Criar5$Params {
      body: UserAdminInput
}

export function criar5(http: HttpClient, rootUrl: string, params: Criar5$Params, context?: HttpContext): Observable<StrictHttpResponse<UserPasswordResetOutput>> {
  const rb = new RequestBuilder(rootUrl, criar5.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserPasswordResetOutput>;
    })
  );
}

criar5.PATH = '/api/admin/users';
